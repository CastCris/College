/*
 * O seguinte código gera um erro proposital.
 * Isso acontece porquê quando digitamos aspas duplas(") estamos declarando uma constante
 * de string, entretanto estamos atribuindo essa constante para uma variável do tipo char.
 *
 * Para concertar isso, basta trocar o tipo char da variável letra para String
 */
